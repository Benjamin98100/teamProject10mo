import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'breaking_bad',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
